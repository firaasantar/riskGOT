function showCountryInfo(countryName) {
    // Replace the following alert with code to display the information you want

    let t = document.getElementById(countryName).innerText;
    alert("Information about " + t);
}